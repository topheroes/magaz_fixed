import React from 'react';
import { StyleSheet, Text, View, Button } from 'react-native';

const Cart = (props) =>
{
    return (
        <View>
            
        </View>
    );
}

export default Cart;