import React from 'react';
import { StyleSheet, Text, View, Button } from 'react-native';

const Orders = (props) =>
{
    return (
        <View>
            
        </View>
    );
}

export default Orders;