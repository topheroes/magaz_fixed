import React from 'react';
import { StyleSheet, Text, View, Button } from 'react-native';

const Orders = (props) =>
{
    return (
        <View>
            <Text>Orders</Text>
        </View>
    );
}

export default Orders;